# e:\holdVisible\config.py

# ==========================================
# 投资组合可视化图表配置项 (Chart Configuration)
# ==========================================

CHART_CONFIG = {
    # 柱子的宽度比例 (占据总可用宽度的百分比)
    # 数值越大(如 '80%')柱子越粗，间隙越小；数值越小(如 '20%')柱子越细，间隙越大。
    "barWidth": "60%",

    # 多系列柱状图的间隙设置
    "barGap": "20%",

    # 柱子内部文字的字体大小 (像素值)
    "labelFontSize": 12,

    # 标签显示的阈值 (小数形式，如 0.04 表示 4%)
    # 当某项资产在当天的占比低于此值时，为了防止文字拥挤，柱子内将只展示资产名称。
    "labelThreshold": 0.04,
    
    # 彻底隐藏并剔除的阈值 (小数形式，如 0.01 表示 1%)
    # 当某项资产占比低于此值时，它将不会被绘制在大图的柱子中，只在悬停及明细表中存在。
    "hideThreshold": 0.01,

    # 顶部堆积柱状图的总高度 (vh 单位)
    # 100 代表一个屏幕高度。如果设置为 200，则图表高度是屏幕的两倍，适合展示大量的细小仓位。
    "heightVh": 100,

    # 清仓连线的垂直延伸长度 (以Y轴“万”为基础刻度)
    # 当发生清仓时，会在对应柱子上统一拉一条垂直虚线往上。此数值决定了虚线的长度。
    "liquidationArrowLength": 120,

    # 是否在柱状图中展示详细信息 (股数和市值)
    # True 为展示，False 为仅展示名称和占比
    "showLabelDetails": True
}